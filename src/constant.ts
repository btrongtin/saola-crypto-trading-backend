// Default page size for pagination
const DEFAULT_PAGE_SIZE: number = 20;

// Cache Time-To-Live (TTL) in milliseconds
const CACHE_TTL: number = 60 * 1000; // 1 minute

export { DEFAULT_PAGE_SIZE, CACHE_TTL };
